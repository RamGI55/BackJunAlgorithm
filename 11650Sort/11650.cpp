#include <iostream> 
#include <algorithm>
#include <vector> 

struct pos{ 

    int x; 
    int y;
}; 

using namespace std; 

int main (){
    int a; 
    cin >> a; 
    for (int i = 0 ; i < a ; i ++ )
    { 
       
    }

    sort(v.begin(), v.end());
    sort(v2.begin(), v2.end());

    for ( int j = 0 ; j < a ; j ++)
    {
        cout << v[j] << v2[j] << "\n"; 
    }
    
    return 0; 

}